<%= render "shared/context_nav", title: t("activerecord.models.#{IProduto.model_name.i18n_key}.other"), new_path: new_i_produto_path do  %>
<span class="breadcrumb-item active"><%= t("activerecord.models.#{IProduto.model_name.i18n_key}.other") %></span>
<% end %>
<div class="card">
  <div class="card-header d-flex">
    <%= search_form_for @q, html: { class: "d-flex gap-2" , autocomplete: "off"} do |f| %>
      <%= f.search_field :nome_cont, class: "form-control", placeholder: "Pesquisar" %>
      <%= f.button(type: 'submit', class: 'btn btn-info') { tag.i(class: 'ph-magnifying-glass') } %>
      <%= link_to  tag.i(class: 'ph-x-circle'), i_produtos_path, class: "btn btn-light" if params[:q] %>
      <% end %>
    </div>
    <div class="card-body">
      <div class= "table-responsive">
        <table class="table table-sm table-bordered table-striped">
          <thead>
            <tr>
              <th class="text-center">Ações</th>
              
                
                  <th><%= sort_link(@q, :nome, t("activerecord.attributes.i_produto.nome")) %></th>
                
                
                  <th><%= sort_link(@q, :slug, t("activerecord.attributes.i_produto.slug")) %></th>
                
                
                  <th><%= sort_link(@q, :descricao, t("activerecord.attributes.i_produto.descricao")) %></th>
                
                
                  <th><%= sort_link(@q, :preco, t("activerecord.attributes.i_produto.preco")) %></th>
                
                
                  <th><%= sort_link(@q, :g_categoria, t("activerecord.attributes.i_produto.g_categoria")) %></th>
                
                
                  <th><%= sort_link(@q, :arquivo_url, t("activerecord.attributes.i_produto.arquivo_url")) %></th>
                
                
                  <th><%= sort_link(@q, :imagem_url, t("activerecord.attributes.i_produto.imagem_url")) %></th>
                
                
                  <th><%= sort_link(@q, :status, t("activerecord.attributes.i_produto.status")) %></th>
                
                
                
                
            </tr>
          </thead>
          <tbody>
            <% @i_produtos.each do |i_produto| %>
            <tr>
              <td class="text-center">
                <div class="dropdown-toggle">
                  <a href="#" class="text-body dropdown" data-bs-toggle="dropdown" aria-expanded="false">
                    <i class="ph-gear"></i>
                  </a>
                  <div class="dropdown-menu" style="">
                    <div class="dropdown-header">Opções</div>
                    <%= link_to edit_i_produto_path(i_produto), class: "dropdown-item" do %>
                    <i class="ph-pen me-2"></i> Editar
                    <% end %>
                    <%= form_for i_produto, method: :delete do |f| %>
                    <a class="dropdown-item hand-pointer " data-confirm-delete="<%= i_produto_path(i_produto) %>" data-csrf-token="<%= form_authenticity_token %>">
                      <i class="ph-trash-simple me-2"></i>Excluir
                    </a>
                    <% end %>
                      
                        <td><%= i_produto.nome %></td>
                      
                      
                        <td><%= i_produto.slug %></td>
                      
                      
                        <td><%= i_produto.descricao %></td>
                      
                      
                        <td><%= i_produto.preco %></td>
                      
                      
                        <td><%= i_produto.g_categoria %></td>
                      
                      
                        <td><%= i_produto.arquivo_url %></td>
                      
                      
                        <td><%= i_produto.imagem_url %></td>
                      
                      
                        <td><%= i_produto.status %></td>
                      
                      
                      
                      
                  </div>
                </div>
              </td>
            </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
    <div class="card-footer">
      <%= pagy_bootstrap_nav(@pagy, link_extra: 'class="btn btn-outline-secondary btn-sm mx-1"').html_safe %>
    </div>
  </div>
