<%= render "shared/context_nav", title: t("activerecord.models.i_curso.other"), new_path: new_i_curso_path do %>
  <span class="breadcrumb-item active"><%= t("activerecord.models.i_curso.other") %></span>
<% end %>
<div class="card">
  <div class="card-header d-flex justify-content-between align-items-center">
    <h2 class="card-title"><%= t("activerecord.models.ICurso.other") %></h2>
    <div>
      <%= link_to new_i_curso_path, class: "btn btn-sm btn-primary" do %>
        <i class="lucide-plus-circle"></i> Incluir
      <% end %>
    </div>
  </div>
  <!-- Filtro com Ransack -->
  <div class="card-body">
    <%= search_form_for @q, url: i_cursos_path, method: :get, html: { class: "mb-3" } do |f| %>
      <div class="row">
        <div class="col-md-4">
          <%= f.label :nome_cont, t("activerecord.attributes.i_curso.nome") %>
          <%= f.search_field :nome_cont, class: "form-control", placeholder: "Pesquisar por nome" %>
        </div>
        <div class="col-md-4">
          <%= f.label :descricao_cont, "Descrição" %>
          <%= f.search_field :descricao_cont, class: "form-control", placeholder: "Pesquisar por descrição" %>
        </div>
        <div class="col-md-4 d-flex align-items-end">
          <%= f.submit "Filtrar", class: "btn btn-sm btn-primary me-2" %>
          <%= link_to "Limpar", i_cursos_path, class: "btn btn-sm btn-secondary" if params[:q] %>
        </div>
      </div>
    <% end %>
  </div>
  <!-- Tabela -->
  <div class="card-body">
    <div class="table-responsive" style="height: 400px; overflow-y: auto;">
      <table class="table table-sm table-bordered table-striped">
        <thead>
          <tr>
            <th class="text-center">Ações</th>
            <th><%= sort_link(@q, :nome, t("activerecord.attributes.i_curso.nome")) %></th>
            <th><%= sort_link(@q, :slug, t("activerecord.attributes.i_curso.slug")) %></th>
            <th><%= sort_link(@q, :descricao, t("activerecord.attributes.i_curso.descricao")) %></th>
            <th><%= sort_link(@q, :preco, t("activerecord.attributes.i_curso.preco")) %></th>
            <th><%= sort_link(@q, :g_categoria, t("activerecord.attributes.i_curso.g_categoria")) %></th>
            <th><%= sort_link(@q, :url_externa, t("activerecord.attributes.i_curso.url_externa")) %></th>
            <th><%= sort_link(@q, :status, t("activerecord.attributes.i_curso.status")) %></th>
          </tr>
        </thead>
        <tbody>
          <% @i_cursos.each do |i_curso| %>
            <tr>
              <td class="text-center">
                <div class="dropdown">
                  <a href="#" class="dropdown-toggle text-body" data-bs-toggle="dropdown" aria-expanded="false">
                    <i class="ph-gear"></i>
                  </a>
                  <div class="dropdown-menu">
                    <div class="dropdown-header">Opções</div>
                    <%= link_to edit_i_curso_path(i_curso), class: "dropdown-item" do %>
                      <i class="ph-pen me-2"></i> Editar
                    <% end %>
                    <%= button_to i_curso_path(i_curso), method: :delete, data: { confirm: "Tem certeza que deseja excluir?" }, class: "dropdown-item text-danger" do %>
                      <i class="ph-trash-simple me-2"></i> Excluir
                    <% end %>
                  </div>
                </div>
              </td>
              <td><%= i_curso.nome %></td>
              <td><%= i_curso.slug %></td>
              <td><%= i_curso.descricao %></td>
              <td><%= i_curso.preco %></td>
              <td><%= i_curso.g_categoria %></td>
              <td><%= i_curso.url_externa %></td>
              <td><%= i_curso.status %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
  <!-- Paginação -->
  <div class="card-footer">
    <%= pagy_bootstrap_nav(@pagy, link_extra: 'class="btn btn-outline-secondary btn-sm mx-1"').html_safe %>
  </div>
</div>
<!-- Estilo -->
<style>
  .table-responsive {
    height: 400px;
    overflow-y: auto;
  }
</style>
