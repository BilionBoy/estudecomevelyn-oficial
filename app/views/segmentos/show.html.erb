<div class="container">
  <%= render "shared/notice" %>
  <div class="card border-0 shadow shadow-sm">
    <div class="card-header border-0 d-flex justify-content-between">
      <span class="fs-4">Visualizar Segmento</span>
    </div>
    <div class="card-body">
      <div id="<%= dom_id @segmento %>">
          <p>
            <strong>Nome:</strong>
            <%= @segmento.nome %>
          </p>
          <p>
            <strong>Descricao:</strong>
            <%= @segmento.descricao %>
          </p>
      </div>
    </div>
    <div class="card-footer border-0 d-flex justify-content-between">
      <div>
        <%= link_to edit_segmento_path(@segmento), class: "btn btn-primary" do %>
        <i class="bi bi-pencil-square"></i> Editar
        <% end %>
        <%= link_to "Voltar", segmentos_path, class: "btn btn-secondary" %>
      </div>
      <%= button_to "Deletar", @segmento, method: :delete, class: "btn btn-outline-danger border-0" %>
    </div>
  </div>
</div>
